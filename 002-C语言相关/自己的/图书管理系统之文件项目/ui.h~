//1.预处理语句
//函数声明
//结构体定义
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
struct _STBOOKINFO
{
    char szName[64];  //书名
    char szAuthor[48];//作者
    char szPress[64]; //出版社
    int dPrice;       //定价
    int iCount;       //库存量
    
}stBookInf[100];
typedef struct _USERINFO
{
    int ID;
    char sName[48];
    char sPasswd[60];
    int Level;
    char sTelPhone[60];
}USERINFO;
USERINFO UserInfo;
struct _STBOOKINFO stBookInfo;
int LoginUI();           //登陆界面
int MainUI();            //主界面
int AddBookUI();         //添加图书界面
int showBookUI();        //查看图书信息
void delete_bookinfo();  //删除图书信息
void ModifyBook();       //更改图书信息

int AddUser();           //添加会员信息
int ShowUser();          //查看会员信息
void DeleteUser();       //删除会员信息
void ModifyUser();       //更改会员信息
FILE *fp,*fp1;






