1.发送sql语句时,给变量的字节数不够导致向数据库插入数据时错乱
2.在if后面多加了一个;找了好久都没有发现
3.*** Error in `./1': double free or corruption (fasttop): 0x000000000215f630 ***
已放弃 (核心已转储)

释放结果集
extern 的使用,
extern MYSQL *conn;
extern MYSQL_RES *res;
extern MYSQL_ROW row;
之前没有加extern时,要么每个使用到数据库的成员函数的地方,都要写一次,要么就是重复定义,使用了extern之后就解决了这个问题
