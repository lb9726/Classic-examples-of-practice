grep 叫动态全局搜索
grep的几个参数
grep（global search regular expression(RE) and print out the line，全面搜索正则表达式并把行打印出来）是一种强大的文本搜索工具，它能使用正则表达式搜索文本，并把匹配的行打印出来。

grep经常和管道符号连用

常用的几个参数
grep -nr 
grep -nRai --include="*.qml" "lineHeight"
在所有qml文件中，搜索lineHeight的关键字

grep -nRai --include="*.cpp" "QLinearGradient"
